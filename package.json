{
  "name": "certifypro-client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "start": "npx vite --port 3000",
    "build": "npx vite build --outDir dist",
    "build:clean": "rm -rf dist && npx vite build",
    "deploy": "npm run build && npx netlify deploy --prod --dir=dist",
    "preview": "vite preview"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "papaparse": "^5.5.3",
    "react": "^18.2.0",
    "react-dom": "^18.3.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.1.4"
  }
}